<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/calm_background">

    <!-- RecyclerView for joined groups -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_posts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"
        android:visibility="gone" />

    <!-- No groups message -->
    <TextView
        android:id="@+id/tv_no_posts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="No groups joined yet.\nGo to Groups tab to join!"
        android:textSize="16sp"
        android:textColor="@color/calm_primary"
        android:textAlignment="center"
        android:visibility="gone" />

    <!-- Loading spinner container -->
    <LinearLayout
        android:id="@+id/loading_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@color/calm_background"
        android:visibility="visible">

        <ProgressBar
            android:id="@+id/loading_spinner"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:indeterminateTint="@color/calm_primary"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Loading your groups..."
            android:textSize="16sp"
            android:textColor="@color/calm_primary" />
    </LinearLayout>

</FrameLayout>
